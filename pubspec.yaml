name: driving_school_app
description: "🚗 Driving School App - A Flutter-based app for driving students with video lessons and AI-powered face detection to ensure engagement. Videos pause when the user looks away and resume upon detection. Secure login, smooth UI, and structured learning experience 📲🎥👀."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.5.1
  cupertino_icons: ^1.0.8
  camera: ^0.11.1
  google_mlkit_face_detection: ^0.12.0
  http: ^1.3.0
  logger: ^2.5.0
  path_provider: ^2.1.5
  permission_handler: ^11.4.0
  path: ^1.9.1
  get: ^4.7.2

dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/videos/siu.mp4
  uses-material-design: true

flutter_icons:
  image_path: "assets/images/car.png"
  android: true
  ios: true